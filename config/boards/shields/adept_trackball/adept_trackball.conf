# Adept trackball peripheral — PMW3610 (badjeff -alt driver) tuning.
# Roles (ZMK_SPLIT / ZMK_POINTING) are set in Kconfig.defconfig.

CONFIG_SPI=y
CONFIG_INPUT=y
CONFIG_PINCTRL=y

# Split peripheral: no USB HID output (ZMK_USB requires a non-split or central
# role; leaving the board default y aborts the Kconfig on current ZMK).
CONFIG_ZMK_USB=n

# PMW3610 optical sensor (badjeff fork -> *_ALT symbols)
CONFIG_PMW3610_ALT=y
# CONFIG_PMW3610_ALT_INVERT_X=y

CONFIG_PMW3610_ALT_REST1_SAMPLE_TIME_MS=20
CONFIG_PMW3610_ALT_REST2_SAMPLE_TIME_MS=200
CONFIG_PMW3610_ALT_REST3_SAMPLE_TIME_MS=300
CONFIG_PMW3610_ALT_RUN_DOWNSHIFT_TIME_MS=500
CONFIG_PMW3610_ALT_REST1_DOWNSHIFT_TIME_MS=3000
CONFIG_PMW3610_ALT_REST2_DOWNSHIFT_TIME_MS=30000

# Match the proven standalone AdeptBLE.conf EXACTLY: 12 ms (~83 Hz) reports.
CONFIG_PMW3610_ALT_REPORT_INTERVAL_MIN=12
CONFIG_PMW3610_ALT_SMART_ALGORITHM=y

# BLE tuning identical to AdeptBLE.conf: +8 dBm TX, EXPERIMENTAL_CONN, and
# ENFORCE_SUBSCRIPTION. AdeptBLE sets NO peripheral-preferred connection params,
# so we set none either -- the central drives the interval. (The dongle-only
# MIN/MAX=6 pin and PREF_LATENCY were removed; LATENCY=30 had broken input-split
# motion entirely, so we stay byte-for-byte with the known-good AdeptBLE here.)
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y
CONFIG_BT_GATT_ENFORCE_SUBSCRIPTION=y
